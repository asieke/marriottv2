import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
	// const sentences = await supabase.getSentences();
	// const photos = await googleClient.getPhotos(user?.user_metadata.album.id || '');
	// return {
	// 	serverData: {
	// 		sentences: sentences,
	// 		photos: photos
	// 	}
	// };
};
